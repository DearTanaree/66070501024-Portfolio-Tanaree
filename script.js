let section = document.querySelectorAll('section');
let navLink = document.querySelectorAll('header nav a');

window.onscroll = () => {
    section.forEach(se =>{
        let top = window.scrollY;
        let offset = sec.offsetTop;
        let height = sec.offsetHright;
        let id = sec.getAttribute('id');
    });
};

